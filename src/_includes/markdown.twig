<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/logo.png" />
    {% include 'scripts.twig' %}
    <title>{{ title }} | TEA Stack</title>
  </head>

  <body class="{{ env.devMode ? 'debug-screens' }} relative bg-zinc-800 text-slate-200  font-roboto">
    <div class="flex flex-col h-screen ">
      {% include 'header.twig' %}
      <div class="text-inherit grow">
        <div class="bg-cover bg-fixed bg-center flex items-center justify-center h-full" style="background-image: url('{{img}}');" >
          <h1 class="text-center {{title_color == 'dark' ? 'text-stone-800' : 'text-stone-100' }} font-bold lg:text-9xl md:text-7xl text-5xl align-middle">{{title}}</h1>
        </div>
      </div>
    </div>
    <div class="markdown p-10">
      {{ content | raw }}
    </div>
  </body>
</html>
